@using TECH.Areas.Admin.Models;
@model List<SidersModelView>
@if (Model != null && Model.Count() > 0)
{

    @*<section id="home-section">
        <div class="container">
            <div class="home-slider-2 owl-carousel">
                @foreach (var item in Model)
                {
                      <div class="slider-item">
                    <div class="item-img" style="background:url('/product-siders/@item.image')">
                        </div>
                    </div>
                }
              
            </div>
        </div>
    </section>*@
    <div class="owl-carousel owl-theme owl-carousel-setting">
        @foreach (var item in Model)
        {
            <div class="item"><img src="/website/images/@item.image" class="d-block w-100" alt="..."></div>            
        }
      @*  <div class="item"><img src="/website/images/slideshow_1.jpg" class="d-block w-100" alt="..."></div>
        <div class="item"><img src="/website/images/slideshow_2.jpg" class="d-block w-100" alt="..."></div>*@
    </div>


}



